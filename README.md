# Vitajte v repozitáry 3web_mm_greg! 👋



## 🚀 Prvé kroky s Gitom

### 1. Nastavenie používateľského mena

Pred prvým použitím Gitu je potrebné nastaviť vaše meno:

```bash
git config --global user.name "Vaše Meno Priezvisko"
```

**Príklad:**
```bash
git config --global user.name "Ferko Mrkvička"
```

### 2. Nastavenie emailovej adresy

Nastavte email, ktorý používate na GitHube:

```bash
git config --global user.email "vas.email@example.com"
```

**Príklad:**
```bash
git config --global user.email "ferko.mrkvicka@example.com"
```

